# Bundled Memory Review: v0.5

Reviewed: 2026-09-22
Scope: six bundled semantic and procedural memory cards.
Implementation plan: `2026-09-22-template-reliability-v0-5.md` under `.agent/plans/`
Baseline revision: `ddc2831131c949780b2abc3d966c3f357359286e`

## Provenance

The six existing cards pointed to `.agent/tasks/README.md`, a scaffold reference,
not a recorded originating task. Those historical values are preserved. This
record documents the current source review; it does not reconstruct earlier
task history or assert that undocumented historical verification occurred.

## Review

- Project facts: checked the v0.5 manifest, context configuration, task workflow,
  memory policy, and reliability evaluation documentation.
- Conventions: checked current root instructions, search/output policy, memory
  rules, guardrail guidance, and durable planning requirements.
- Decisions: checked current runtime, skill versioning, manual memory promotion,
  and ADR 0004 context authority/storage rules. Removed an unsupported historical
  attribution rather than retaining it as a verified current fact.
- Debugging: checked the focused reproduce/fix/re-test skill, output policy, and
  available verification commands.
- Testing: checked verification commands and the distinction between offline
  harness checks and measured live outcomes.
- Refactoring: checked architecture/search guidance and verification commands.

The index lists reviewed source paths and normalized UTF-8 SHA-256 fingerprints.
These fingerprints detect later content drift; they are not proof that claims
are true or that the workflows improve model performance. Sources modified
during integration must be reviewed again before their fingerprints are refreshed.

## Validation

- `uv run --no-project --with 'pytest>=8,<9' python -m pytest tests/agent/test_memory_evidence.py -q`:
  21 passed, 1 skipped because this Windows environment could not create a symlink.
  Coverage includes normalization, drift, missing/unsafe/malformed sources, legacy
  entries, strict promotion validation, and unreviewed candidates.
- `python scripts/validate_memory_links.py --require-evidence` and
  `python scripts/audit_memory_staleness.py`: six reviewed cards reported unchanged
  at this review point. Integration changes require another source review and audit.
- Live behavioral effectiveness is not measured by this review.

## Final Integration Review

On 2026-09-22, rechecked the final manifest against the project-facts and decisions
cards. The added copy-if-missing verification record preserves existing memory
ownership; the v0.5 version, ten-skill catalog, runtime priority, and opt-in
guardrails still support both cards. Refreshed only their manifest fingerprints.
Other evidence remained unchanged. Independent review also verified legacy
Windows metadata compatibility without relaxing the evidence path policy.
