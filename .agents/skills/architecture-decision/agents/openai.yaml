interface:
  display_name: "Architecture Decision"
  short_description: "Compare technical options and record decisions"
  default_prompt: "Use $architecture-decision to compare these options and record the recommended decision."
policy:
  allow_implicit_invocation: true
