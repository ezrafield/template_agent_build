interface:
  display_name: "Code Review"
  short_description: "Review changes for correctness, risk, and tests"
  default_prompt: "Use $code-review to review these changes for correctness, risk, and missing tests."
policy:
  allow_implicit_invocation: true
