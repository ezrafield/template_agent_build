interface:
  display_name: "Repository Navigator"
  short_description: "Locate relevant code, tests, and project context"
  default_prompt: "Use $repo-navigator to locate the code, tests, and documentation relevant to this task."
policy:
  allow_implicit_invocation: true
