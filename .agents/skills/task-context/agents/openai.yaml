interface:
  display_name: "Task Context"
  short_description: "Build and explain routed Markdown task context"
  default_prompt: "Use $task-context to build or explain the routed Markdown context bundle for this task."
policy:
  allow_implicit_invocation: true
