interface:
  display_name: "Test Scope"
  short_description: "Keep the smallest sufficient regression suite"
  default_prompt: "Use $test-scope to select the necessary tests and reduce redundant coverage."
policy:
  allow_implicit_invocation: true
