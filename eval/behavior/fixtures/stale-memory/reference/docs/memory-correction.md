# Proposed memory correction

Current authority: docs/cache-contract.md. Expiry is elapsed >= TTL, in seconds; negative TTL expires immediately. cache-policy.md contains a stale boundary claim. cache-policy-copy.md is a duplicate. Propose re-verifying cache-policy.md against the contract and retiring the duplicate after review. No memory is automatically promoted or deleted.
